# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                               controllers.Application.index()

# Authentication
# ~~~~
GET     /login                          controllers.Authentication.login()
POST    /auth                           controllers.Authentication.authenticate()
GET     /logout                         controllers.Authentication.logout()

# Day
# ~~~~
GET     /cra/:trigramme/:year/:month/:day   controllers.Days.fetch(trigramme: String, year: Int, month: Int, day: Int)

# Cra
# ~~~~
GET     /cra/:trigramme/:year/:month    controllers.JCras.fetch(trigramme: String, year: java.lang.Integer, month: java.lang.Integer)

# HtmlPage
# ~~~~
GET     /unsupportedBrowser             controllers.HtmlPages.unsupportedBrowser

# Javascript routing
# ~~~~
GET     /assets/javascripts/routes      controllers.Application.javascriptRoutes()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file                   controllers.Assets.at(path="/public", file)
GET     /public/*file                   controllers.Assets.at(path="/public", file)
